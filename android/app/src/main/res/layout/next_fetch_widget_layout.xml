<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/next_fetch_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/widget_background"
    android:clickable="true"
    android:focusable="true">

    <!-- Left: Pie / Ring -->
    <ImageView
        android:id="@+id/next_fetch_ring"
        android:layout_width="88dp"
        android:layout_height="88dp"
        android:contentDescription="Countdown ring"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:layout_marginEnd="12dp"/>

    <!-- Right: Text content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/next_fetch_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="⏰ Next Fetch in"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#000000"/>

            <TextView
                android:id="@+id/next_fetch_countdown"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Calculating..."
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#4B4D73"/>
        </LinearLayout>

        <!-- small spacer -->
        

        <TextView
            android:id="@+id/next_fetch_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ready to fetch"
            android:textSize="12sp"
            android:textColor="#56607A"/>

        <TextView
            android:id="@+id/next_fetch_timestamp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🗓️ 11 Nov - 12 Nov"
            android:textSize="11sp"
            android:textColor="#8A97B0"
            android:layout_marginTop="4dp"/>

    </LinearLayout>
</LinearLayout>
